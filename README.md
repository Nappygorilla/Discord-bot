# NoContext Discord Bot

Standalone Discord bot for the NoContext website.

## Render

- Runtime: Node
- Build command: `npm install`
- Start command: `npm start`

Set the variables from `.env.example` in Render. Keep bot tokens and secrets out of GitHub.
